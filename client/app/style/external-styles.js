import React, {
  StyleSheet
} from 'react-native';

const externalStyles = StyleSheet.create({
  storyTitle: {
    fontSize: 36,
    fontWeight: 'bold',
    color: '#5379ae'
  }
});

module.exports = externalStyles;
